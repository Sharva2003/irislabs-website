# Booking form email delivery (optional).
# Without these, submissions are validated and accepted but only logged
# server-side — nothing is emailed. Get a key at https://resend.com.
RESEND_API_KEY=
BOOKING_NOTIFY_EMAIL=hello@irislabs.dev
